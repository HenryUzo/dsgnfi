# Local-only secrets were removed from the tracked repository.
# Create an untracked .env.local with real deployment credentials when needed.
CONTACT_FROM_EMAIL="Dsgnfi Support <support@example.com>"
RESEND_API_KEY="replace_me"
VERCEL_OIDC_TOKEN="replace_me"
